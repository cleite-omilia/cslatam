{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "Vite Dev (Frontend only)",
      "runtimeExecutable": "bash",
      "runtimeArgs": ["-c", "export NVM_DIR=$HOME/.nvm && . $NVM_DIR/nvm.sh && npm run dev"],
      "port": 5173
    },
    {
      "name": "Wrangler Pages Dev (Full-stack)",
      "runtimeExecutable": "bash",
      "runtimeArgs": ["-c", "export NVM_DIR=$HOME/.nvm && . $NVM_DIR/nvm.sh && npm run build && npx wrangler pages dev dist --d1=DB --persist-to=.wrangler/state --port 8788"],
      "port": 8788
    }
  ]
}
